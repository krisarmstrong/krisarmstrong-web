# Theming Guide

- Token-usage canaries added; ThemeDiagnostics added; canaries surface token colors in header area.
- Override tokens at page level; add new tokens to shared-tokens.css and consume via TS tokens.
- Migration path: consider a shared token package to standardize across apps.
