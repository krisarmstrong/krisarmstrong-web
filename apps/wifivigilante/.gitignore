# Dependencies
/node_modules

# Build output
/build
/dist
/.vite # Vite cache directory
/.parcel-cache # Parcel cache directory
/.next # Next.js build output

# Environment variables
# Ignore all .env files by default
.env
.env.*
# Except for template/example files that should be committed
!/.env.example
!/.env.template
# All environment-specific files must be ignored (contain real credentials)
.env.development
.env.staging
.env.production
# Specifically ignore local environment overrides
.env.local
.env.development.local
.env.production.local
.env.test.local

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# IDE / Editor specific files
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Test runner reports & coverage
/coverage
/reports
junit.xml

# Optional: Yarn cache directory (if using Yarn PnP or similar)
# .yarn/cache
# .yarn/unplugged
# .yarn/build-state.yml
# .yarn/install-state.gz
# .pnp.*

# Optional: TypeScript build output (if not included in your main build/dist)
# *.tsbuildinfo
src/Backup/2025_May_Wi-Fi_Vigilante_website_backup.zip
wifi-vigilanti-casefiles-app
src/Backup/2025_May_Wi-Fi_Vigilante_website_backup.zip
